# これは音楽用 DSP の算術ライブラリです
目的の関数は sin, cos, log, exp, pow です。
各関数は constexpr であることが前提です。
底が既知の場合は constexpr として計算されるように最適化すること。
inline 展開はフル活用すること。
ランタイムで std より速く(floatなら16bit精度, double なら24bit精度)、コンパイル時はまずまずな速度である程度精度を高くしようという感じです。
C++20までの機能を使っていいです。
割り算はコンパイル時計算で掛け算にできるようならコンパイラが展開しやすいようにしてください。
SIMDが使えそうな部分では使ってください。各環境で動くようにインクルードガードを適切に行うこと。
アルゴリズムも変更していいです。
方針は次の通りです。

1. まずは解析的努力により計算が必要な変域を狭めてなるべく constexpr に逃がす
2. テイラー展開を使うのではなく、L1キャッシュに乗る程度のLUTを使って補間する。
  - その際、必ずしも LUT の取り方が線形であるのが望ましいとも限らないことに注意すること
  - その際、線形補間で精度が間に合わなかったり LUT が膨れすぎるようなら2次近似その他のアルゴリズムを使うこと

## 解析的努力の例

e^x = 2^{x*logx(e)} を使って指数関数を constexpr * ビットシフト * 半径0.5の近似 にする
log(x) を log2 に帰着し(底の変換公式(変換はなるべく constexpr に逃がす)), 仮数部と指数部を分ける (対数法則), メビウス変換を使って 近似の直径を 1/3 まで減らす

## 原則について
私が書いた原則より精度・速度的に望ましい方法があるのだとしたら、また実験により確認できたのであれば、この原則よりそちらを優先してください。

## ディレクトリ構成
tests/ パフォーマンステストが入っています
dspmath.hpp これが目的のものです

## 現行実装の設計と最適化 (2026-09-20 ソース確認)

以下は `dspmath.hpp` と `tests/compare_std.cpp` を照合した現行仕様である。
後段の 2026-07-08 レポートは履歴であり、現在の経路や測定条件については
本節を優先する。2026-09-20 に比較実験を行い、double log2 の LUT 化と
float sin の補間係数前計算を採用した。条件・結果・再現手順は
[`tests/optimization-results.md`](tests/optimization-results.md) に記録する。

### 精度の定義と適用範囲

テストの `max_rel` は純粋な相対誤差ではなく、
`abs(actual - expected) / max(abs(expected), 1)` の最大値である。
出力の絶対値が 1 以下では絶対誤差、それより大きければ相対誤差になる。
合格条件は float が `1.5e-5`、double が `6.0e-8` と、関数ごとの絶対誤差上限。
「16bit / 24bit」はこの指標に対する目標であり、全入力での有効桁数保証ではない。

現行テストは各 44,100 点で、三角関数は ±8τ、log 系は [0.001, 256]、
exp は [-10, 10]、pow は底 [0.05, 16] と指数 [-3, 3] を同じ添字で
組み合わせている。pow の全組み合わせや広い指数域は検証していない。
固定底の計測は底 2 のみである。追加の `optimization_accuracy.cpp` では
log2 の仮数・広い指数域・LUT 境界の隣接値、三角関数の境界とバイアス丸め境界、
pow の底 [0.05,16] × 指数 [-64,64] の独立な組合せを検証する。

ビット分解は IEEE 754 binary32 / binary64 の表現を前提とする。
log 系は正の正規化数を想定しており、ゼロ・負数・非正規化数・NaN・無限大の
標準数学関数と同等の処理は実装されていない。pow の負の底も同様。
exp2 コアはアンダーフロー側をゼロへ落とし、`exp(double)` は ±709 の外側を
ゼロまたは無限大に制限する。三角関数の整数変換も有限の範囲を前提とする。
これらを含む全域で `std` と互換だと記述してはいけない。

### 共通設計

- **変域縮小を先に行う。** 周期・対数法則・指数部と仮数部の分離で近似区間を
  小さくし、その区間に必要な次数だけを使う。LUT は目的ではなく、演算数・
  依存鎖・テーブル読出しの費用を交換する手段である。
- **定数評価と実行時の経路を分ける。** `std::is_constant_evaluated()` により
  sin / cos / exp / 同型 pow の経路を選ぶ。LUT は `inline constexpr` で生成し、
  実行時の初期化や動的確保を不要にする。生成用の長い級数はホットパスにない。
- **Horner 法と積和。** `detail::mul_add` は定数評価時は `a*b+c`、実行時は
  `FP_FAST_FMA(F)` があれば `std::fma`、なければ `a*b+c` を使う。
  後者が FMA へ縮約されるかはコンパイラとオプション次第であり、命令生成を
  確認する。FMA 有無で丸めが変わるため、双方の精度確認が必要。
- **強制インライン。** 主要な同型オーバーロードとホットパスには
  `DSPMATH_INLINE` を付け、大きい翻訳単位でのインライン失敗を抑える。
  汎用テンプレートの cos / log(base,x) / pow などには付いていない。
  既知の底の畳み込みは最適化を助ける設計であり、`constexpr` や強制インライン
  だけで任意の実行時呼び出しの定数畳み込みを保証するものではない。
- **定数の逆数・指数ビット操作。** ln と log2 の変換は定数乗算、2 の整数乗の
  スケーリングは指数フィールドの更新で行う。float log2 のメビウス変換と
  `log(base,x)` には実行時除算が残る。手書き SIMD intrinsic は現状使っていない。

### 関数別の経路と選択理由

| 関数 | 実行時の手法 | 主な交換条件 |
|---|---|---|
| sin(float) | 256 区間 × 3 係数の float LUT (3 KiB)、二次補間 | 値・傾き・曲率を前計算し、実行時の差分演算を除去 |
| cos(float) | 汎用 cos から π/2 を加えて sin(float) | 表を共有するが、位相加算の丸めも加わる |
| sin / cos(double) | 各 256 点の sin / cos LUT (合計 4 KiB) と加法定理 | 小残差の sin を三次、cos を二次で補正し、長い多項式を避ける |
| log2(float) | 指数・仮数分離、メビウス変換、調整係数を含む奇多項式 | LUT 読出し不要だが除算が 1 回残る |
| log2(double) | 64 区間の逆数・対数 LUT (1 KiB) と四次残差多項式 | 実行時除算を除去。定数評価は従来の 7 項の奇級数を保持 |
| log(float / double) | log2(x) × ln(2) | 底変換の除算を定数乗算に置換 |
| exp(float) | pow_exp2_float(x × log2(e)) | 256 点 float LUT (1 KiB) と二次残差補正を pow と共有 |
| exp(double) | exp2_scaled_core(x × (64 log2(e)) + bias) | 64 点 double LUT (512 B) と三次残差補正。前処理を積和にまとめる |
| pow(float,float) | 2^(exponent × LUT-log2(base)) | 対数は 257 点 float LUT (1,028 B) の線形補間、指数側は上記の共有コア。実行時除算なし |
| pow(double,double) | 2^(exponent × log2(base)) | 実行時は両側 LUT。ln と log2 の往復乗算を排除 |
| 型の異なる pow | common_type で exp(exponent × log(base)) | 同型の専用融合経路とは異なる |

float の二次補間は `current + slope*t + curvature*t*t`、
`slope=(next-previous)/2`、`curvature=(next-2*current+previous)/2`。
この 3 係数を `make_sin_coefficients()` で定数評価し、AoS 形式で保持する。
元の 256 点の値表は係数生成に使い、実行時の補間は係数表を参照する。
double の三角関数は `δ < τ/256` に縮小し、
`sin δ ≈ δ(1-δ²/6)`、`cos δ ≈ 1-δ²/2` を加法定理へ代入する。
cos の打切り項 δ⁴/24 は約 1.5e-8 だが、これは全体の誤差保証ではない。

float log2 と double log2 の定数評価は `x=2^e*m`、`m∈[1,2)` とし、`z=(m-1)/(m+1)∈[0,1/3)`
から `log2(x)=e+2*z*P(z²)/ln(2)` を評価する。
float の P は `1 + z²/3 + z⁴/5 + 0.1543*z⁶` であり、
最終係数は本来の 1/7 を置き換えている。厳密な Remez 法で生成した係数とは
確認できないため、認証済み minimax 係数とは呼ばない。
double は `1,1/3,...,1/13` を使う。
double の実行時は仮数の上位 6 bit で区間を選び、中心
`c=1+(i+0.5)/64` の逆数と log2(c) を constexpr で前計算する。
`r=m/c-1` は絶対値が約 1/129 以下となり、
`log2(x) ≈ e+log2(c)+LOG2E*r*(1-r/2+r²/3-r³/4)` で評価する。
係数は log1p の級数由来であり、Remez 係数ではない。
pow の対数誤差 Δ は結果の相対誤差へ概ね `ln(2)*exponent*Δ` として伝わるため、
log 単体の合格だけを根拠に次数を削らない。

指数コアは `2^t = 2^n * 2^(j/N) * exp(g)` に分解する。
float は N=256 と二次補正、double は N=64 と三次補正であり、両者を
「64 点の共通 LUT」と説明してはいけない。整数変換前に正のバイアスを加え、
正の値の truncation を floor として利用する。float の sin も類似の方法を使う。
ただし浮動小数点のバイアス加算自体に丸めがあるため、元の位相の数学的 floor
と常に一致するわけではない。境界と残差区間は実際の丸めを含めて検証する。
double の三角関数はバイアスではなく、整数への切捨て後に負側を補正する。
「分岐レス」はソースの意図であり、生成命令を確認せず全機種で断言しない。

定数評価では sin に象限縮小と多項式、exp に最近傍整数の ln(2) 倍を引く縮小と
float 五次 / double 七次の多項式を使う。pow は exp と log の合成を使う。
実行時とは経路が違うので、それぞれ検証する。

### 計測結果を解釈するときの注意

`compare_std.cpp` は各呼出しで単一の `volatile sink` に加算する。この依存鎖と
volatile の読み書きが含まれるため、純粋な関数レイテンシでも、独立入力を
最大限並列実行したスループットでもない。これだけで SIMD や短い関数の改善が
無効だと一般化しない。過去の SIMD 不採用は当時の条件に限定した判断とする。

追加した `optimization_benchmark.cpp` は独立した 4 アキュムレータ、依存入力、
64 KiB の競合テーブル付きの 3 モードを分ける。結果は最後に volatile へ渡し、
ウォームアップ後に関数の実行順を入れ替え 9 回の中央値を記録する。
実際の IMFMSynth の処理を測ったものではなく、アプリ全体の高速化率ではない。
今後も最適化による消去を防ぎ、生成命令を確認する。
CPU・コンパイラ・最適化/FMA/fast-math 設定と入力範囲も残す。
`CMakeLists.txt` 自体は AVX2 / fast-math を指定していないため、通常の CMake
ビルドを過去の `/O2 /arch:AVX2 /fp:fast` 計測と同一条件だと扱わない。

## 追加最適化の採否 (2026-09-20)

提案は実装・計測を行い、以下の結論に置き換えた。未検証の提案一覧は残さない。

- **採用:** double log2 の 64 区間 LUT と四次残差多項式。32 区間とほぼ同速で、
  対数の最大誤差が小さく、pow の指数による誤差増幅に余裕を持てる。
- **採用:** float sin の AoS 補間係数 LUT。SoA とほぼ同速で、係数が同じ区間に
  まとまる AoS を選択。cos(float) もこの経路を利用する。
- **不採用:** log2 の再中心化。精度は改善したが、今回の MSVC / Ryzen 環境では
  現行の級数より遅かった。16 区間 LUT は 32 / 64 区間より遅かった。
- **不採用:** sincos・正規化位相・prepared-base の公開 API 追加。
  合成した呼出し条件では sincos の独立入力と正規化位相に明確な利点がなく、
  prepared-base は独立入力で改善した一方、依存入力では悪化した。
  コンパイラによる共通式除去・定数乗算の融合・ループ不変式の移動に依存する。
  実アプリの呼出し条件を確認していない段階で、公開 API は増やさない。

不採用の実験実装は製品ヘッダにもテストソースにも残さず、計測値と判断を
`tests/optimization-results.md` に保存する。既存の公開 API と定数評価経路は維持する。

## 過去の実装レポート (2026-07-08)

以下の数値と判断は当時の記録であり、今回再現測定したものではない。
exp(float) の実行時経路、float log2 の係数説明、SIMD に関する一般化などは
上の現行説明で更新している。

`tests/compare_std.cpp` で精度・速度を計測し、目標精度（float: 相対誤差
≤2⁻¹⁶≈1.5e-5、double: ≤2⁻²⁴≈6e-8）に対して過剰精度だった箇所の LUT・多項式
を間引き、アルゴリズムを改良した。以下、次にこのファイルを触る AI・人間の
両方が状況を把握できるようにまとめる。

### やったこと

1. **過剰精度の間引き**（誤差が目標の何倍余裕があるかを確認してから削減）
   - `sin`/`cos`(double) LUT: 512→256 エントリ（8KB→4KB）。誤差 9e-10→1.5e-8
     で、まだ 4 倍の余裕。
   - `sin`(float) LUT: 1024→256 エントリ（4KB→1KB）。誤差はほぼ不変
     （5.4e-6→5.8e-6）——float 側の誤差はテーブルサイズでなく位相計算の float
     丸めが支配的だったため、テーブルを 1/4 にしても精度が落ちない。
   - `exp`(float) の Horner 多項式: 6 次→5 次。
   - `log2`(double) の級数: 8 項→7 項。
   - `log2`(float) の級数: 5 項→4 項。ただし単純に削ると打ち切り誤差が出るので、
     最終係数を `1/9`→`0.1543` にチューニングし、削った高次項の誤差を吸収
     させた（ミニマックス的発想）。結果、項を減らしたのに誤差は悪化しなかった
     （1.1e-6→1.0e-6）。
   - **教訓**: LUT やテイラー項数を削るときは、必ず「その関数の誤差の支配項は
     何か」を先に特定すること。テーブルサイズを削っても他の誤差要因（丸め等）
     が支配的ならほぼタダで削減できる。逆に係数チューニングは項数を削っても
     精度を維持できる場合がある。

2. **アルゴリズム改良: `exp`/`pow`(double) 用の共有 `2^t` コア**
   - `detail::exp2_scaled_core` / `exp2_lut_double` を新設。
     `2^t = 2^n · 2^(j/64) · e^g` を 64 エントリ（512B）の LUT + 3 次多項式の
     端数補正で計算する。
   - **バイアス・トリック**: floor 計算にデータ依存分岐を使うと大幅に遅くなる
     （実測で 2.7ns→3.9ns に悪化して原因を特定した）。`t*64 + 65536` のように
     常に正の値へオフセットしてから int64 に truncate すると、truncate が
     floor と一致するため分岐が消える。同種の「符号付き floor」を扱うコードを
     書くときはこのトリックを検討すること。
   - `exp(double)` は `x·(log2e·64)+bias` を FMA 1 回でこのコアに直結。
   - `pow(double)` のオーバーロードを追加し、`2^(e·log2 b)` の融合経路にした。
     従来の汎用 `pow` (`exp(e·ln b)` 経由) にあった `ln ↔ log2` 変換の乗算
     2 回を排除。
   - **既知の限界**: `pow(double)` の相対誤差は 3.1e-8 で目標 6e-8 に対し
     余裕は 2 倍しかない。`log2(double)` の誤差は `pow` で指数 `e` 倍に増幅
     されるため、**`log2(double)` をこれ以上間引いてはいけない**。

3. **計算機的改良: MSVC のインライン予算切れを発見・修正**
   - 大きな翻訳単位（`compare_std.cpp` 全体）では、`pow`/`log2` のような
     小関数でも MSVC のインライン予算が尽きてインライン化されないことがある。
     これが起きると **ランタイムの LUT パスだけでなく、底が既知の場合の
     コンパイル時定数畳み込みも黙って無効化される**（このライブラリの目的の
     根幹に関わる）。
   - 単体の小さい翻訳単位でのマイクロベンチマーク（`pow(2.0, x)` が 2.7ns）と、
     `compare_std.cpp` 内での計測（同じ呼び出しが 7.2ns）を比較して発見した。
     速度が想定より悪いのに理由が分からないときは、関数を切り出した最小
     TU で再計測して比較するとよい。
   - `dspmath.hpp` 冒頭に `DSPMATH_INLINE`（MSVC: `__forceinline`、GCC/Clang:
     `inline __attribute__((always_inline))`）マクロを定義し、`detail`
     ホットパス関数と公開 API（`sin`/`cos`/`log`/`log2`/`exp`/`pow`）全てに
     付与した。これにより `pow2`(float) は 5.0ns→2.7ns、`pow2`(double) は
     7.2ns→2.8ns に改善。単一の変更としては今回最大の効果だった。

### 現在のベンチマーク結果（このマシン、MSVC `/O2 /arch:AVX2 /fp:fast`、noisy ±0.05）

`ratio = dsp_ns / std_ns`（小さいほど speed）

| 関数 | float | double |
|---|---|---|
| sin/cos | ~0.71 | ~0.60 |
| log | ~0.98 | ~0.74 |
| log2 | ~0.95 | ~0.46 |
| exp | ~1.0（互角） | ~0.83 |
| pow | ~0.86 | ~0.62 |
| pow（底2） | ~0.51 | ~0.26 |

`exp`(float) のみ std とほぼ互角（この環境の std AVX2 実装が非常に速いため。
過去の実験で LUT 化を試みたが速度で勝てなかった。詳細は git 履歴・過去セッシ
ョンのメモリ参照）。それ以外は全関数が `std` 以下の実行時間で、精度は全て
目標の 16bit/24bit 予算内。

### 次にやるなら

- `log2(double)` はこれ以上削らない（上記の理由）。
- SIMD 化（関数内部の手動ベクトル化）は過去に検証済みで、このベンチマーク
  （スループット律速、呼び出し側でパイプライン化される）には効かないと分かって
  いる。むしろ遅くなる。再挑戦しないこと。呼び出し側ループの自動ベクトル化を
  阻害しない（分岐レス・インライン可能を保つ）のが正しい SIMD 戦略。
- 新しい関数やパスを追加するときは、ホットパスに `DSPMATH_INLINE` を付け忘れ
  ないこと。忘れると大きな TU でインライン化が失敗し、速度・定数畳み込みの
  両方が静かに壊れる。
